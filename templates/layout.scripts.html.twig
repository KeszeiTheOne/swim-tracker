<script src="{{ asset("vendor/jquery/dist/jquery.min.js") }}" type="text/javascript"></script>
<script src="{{ asset("vendor/bootstrap/dist/js/bootstrap.min.js") }}" type="text/javascript"></script>
<script src="{{ asset("vendor/moment/min/moment-with-locales.min.js") }}" type="text/javascript"></script>
<script src="{{ asset("vendor/tempusdominus-bootstrap-4/build/js/tempusdominus-bootstrap-4.min.js") }}"
        type="text/javascript"></script>
<script src="{{ asset("vendor/inputmask/dist/jquery.inputmask.min.js") }}" type="text/javascript"></script>

<script type="application/javascript">
	$(".datetimepicker").datetimepicker({
		format: "YYYY-MM-DD HH:mm",
		locale: moment.locale("hu"),
        icons: {
			time: 'far fa-clock',
        },
		allowInputToggle: true,
        maxDate: new Date(),
		useCurrent: false
		// maxDate:"today"
	});

	// $(".timepicker").inputmask({
	// 	regex: "\\d{0,}:[0-5][0-9]",
	// 	numericInput: true,
	// 	insertMode: false
	// });

	$(".weightMask").inputmask({
		mask: "9{1,3}",
		numericInput: true
	});
	$(".heightMask").inputmask({
		mask: "9{1,3}",
		numericInput: true
	});
	$(".dateMask").inputmask({
		alias: "datetime",
		inputFormat: "yyyy-mm-dd",

	});
	$(".emailMask").inputmask({
		mask: "*{1,20}[.*{1,20}][.*{1,20}][.*{1,20}]@*{1,20}[.*{2,6}][.*{1,2}]",
		greedy: false,
		onBeforePaste: function(pastedValue, opts) {
			pastedValue = pastedValue.toLowerCase();
			return pastedValue.replace("mailto:", "");
		},
		definitions: {
			'*': {
				validator: "[0-9A-Za-z!#$%&'*+/=?^_`{|}~\-]",
				casing: "lower"
			}
		}
	});

</script>